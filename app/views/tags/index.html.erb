<h1>All Tags</h1>

<ul id="tags">
  <% @tags.each do |tag| %>
  <li>
    <%= link_to tag.name, article_path(article), class: 'article_title' %>
  </li>
  <% end %>
</ul>

<%= link_to "Create a New Tag", new_article_path, class: "new_article" %>
